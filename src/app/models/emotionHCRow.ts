export interface EmotionHCRow {
  country: string,
  value: number | null,
  avgAge: number,
  avgConfidence: number | null,
  continent: string,
  numFaces: number,
  percentMan: number,
  percentWoman: number
}
